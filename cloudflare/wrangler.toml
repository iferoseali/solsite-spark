name = "solsite-worker"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

# Production environment
[env.production]
routes = [
  { pattern = "*.solsite.xyz/*", zone_name = "solsite.xyz" },
  { pattern = "solsite.xyz/*", zone_name = "solsite.xyz" }
]

# Development/staging
[env.staging]
routes = [
  { pattern = "*.staging.solsite.xyz/*", zone_name = "solsite.xyz" }
]

# KV namespace for caching (optional, for more control)
# [[kv_namespaces]]
# binding = "SITE_CACHE"
# id = "your-kv-namespace-id"

# Environment variables (set via wrangler secret)
[vars]
CACHE_TTL = "3600"
